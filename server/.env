PORT=5000
NODE_ENV=development
DATABASE_URL="file:./dev.db"

JWT_SECRET="truthlens_jwt_secret_token_key_2026"
JWT_REFRESH_SECRET="truthlens_jwt_refresh_secret_token_key_2026"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# AI Provider API Keys (Optional, system will fallback to automated statistical and local analysis if empty)
GEMINI_API_KEY=""
HUGGING_FACE_API_KEY=""

# SMTP Mail Server Configuration (Optional, falls back to printing to terminal console if not configured)
SMTP_HOST="smtp.ethereal.email"
SMTP_PORT=585
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="TruthLens AI <noreply@truthlens.ai>"

# Client CORS URL
FRONTEND_URL="http://localhost:5173"
